package app.tuti.tj.data.auth

// ════════════════════════════════════════════════════════════════
//  ВИДЫ ОТКАЗОВ ПРИ ВХОДЕ
//
//  Отмена сюда не входит намеренно: человек, закрывший окно выбора
//  аккаунта, ничего не сломал — показывать ему красный текст не за
//  что. Смешай отмену с ошибками в одном типе, и на экране рано или
//  поздно появится ошибка там, где её быть не должно.
// ════════════════════════════════════════════════════════════════

enum class AuthErrorKind {
    /** Нет интернета. */
    NO_NETWORK,

    /** Google Play Services недоступны или устарели. */
    PLAY_SERVICES,

    /** Всё остальное: показываем общий текст и даём связаться с нами. */
    UNKNOWN,
}
